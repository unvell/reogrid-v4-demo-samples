<Window x:Class="unvell.ReoGrid.WPFDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:unvell.ReoGrid.WPFDemo.ViewModels"
        xmlns:beh="clr-namespace:unvell.ReoGrid.WPFDemo.Behaviors"
        xmlns:rg="clr-namespace:unvell.ReoGrid;assembly=unvell.ReoGrid"
        mc:Ignorable="d"
        Title="ReoGrid MVVM Implementation Sample" Height="450" Width="800">
    <Window.DataContext>
        <vm:SpreadsheetViewModel />
    </Window.DataContext>
    <Grid>
        <rg:ReoGridControl x:Name="Grid"
                      beh:ReoGridBindingBehavior.ItemsSource="{Binding Rows}"
                      beh:ReoGridBindingBehavior.EnableTwoWay="True"
                      />
    </Grid>
</Window>
